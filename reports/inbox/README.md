# Inbox

Place your weekly source files here, then run `python main.py` from the project root.

- **Vitel Global**: `*callrecords*.csv` or any `.csv` CDR weekly report  
- **LinkedIn**: `*inmail*.xlsx` or any `.xlsx` InMail status report  

The newest matching file of each type is used when you run with `--no-prompt`.
